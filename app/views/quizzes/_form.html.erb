<%= form_with(model: quiz, local: true) do |form| %>
  <% if quiz.errors.any? %>
    <div id="error_explanation">
			<h2><%= pluralize(quiz.errors.count, "error") %>
			prohibited this book from being saved:</h2>
			
			<ul>
			<% quiz.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
			</ul>
    </div>
  <% end %>

  <div class="field">
		<%= form.label :title %>
    <%= form.text_field :title, id: :quiz_title %>
  </div>

  <p>This is where we implement some kind of editor for the quiz</p>

  <div class="actions">
		<%= form.submit %>
  </div>

<% end %>